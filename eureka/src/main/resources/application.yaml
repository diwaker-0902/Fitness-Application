spring:
  application:
    name: eureka

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

  eureka:
  instance:
    ip-address: 127.0.0.1
    prefer-ip-address: true

server:
  port: 8761